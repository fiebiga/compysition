language: python
python:
  - "2.7.16"
jobs:
  include:
    - name: "Ubuntu 18.04"
      dist: bionic
before_install:
  - sudo apt-get -qq update
  - make dependencies
  - make install-pytest
install:
  - make install
script:
  - pytest