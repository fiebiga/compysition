language: python
jobs:
  include:
    - python: 2.7
      dist: bionic
before_install:
  - sudo apt-get -qq update
  - make dependencies
  - make install-pytest
install:
  - make install
script:
  - pytest